fn foo(mut x: &mut i32) {
    if *x == 0 {
        x = &mut 0;
    } else {
        *x = 1234;
    }
}

#[start]
fn main(_: isize, _: isize) -> isize {
    let mut x = 12;
    foo(&mut x);
    x.isize
}
