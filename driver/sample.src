fn fib(n: isize) -> isize {
    if n <= 1 {
        return n;
    }
    fib(n - 1.isize) + fib(n - 2.isize)
}

extern "C" fn main(argc: isize, _: isize) -> isize {
    let x = fib(36.isize);
    0.isize
}
