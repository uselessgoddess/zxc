fn fib(n: isize) -> isize {
    if n <= 1 {
        return n;
    }
    fib(n - 1.isize) + fib(n - 2.isize)
}

extern "C" {
    fn abort() -> !;
    fn exit(code: i32) -> !;
}


#[start]
fn main(_: isize, _: isize) -> isize {
    64 * 64
}
